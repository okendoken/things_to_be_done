//todo in future it's easier to just rerender src attr of <img/> tag
$("#avatar").replaceWith("<%= escape_javascript(render :partial => "partials/common/avatar",
                            :locals => { :guy => @project, :upload_path => upload_project_path(@project),
                            :link_path => project_path(@project)}) %>");
$('#avatar-upload').fileupload({
    dataType: 'script'
});